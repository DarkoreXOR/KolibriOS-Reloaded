[package]
name = "kolibri_utils"
version = "0.1.0"
edition = "2021"
description = "Freestanding KolibriOS kernel utils (Cuts A–AB): CRC, Unicode, FS, net, GUI, process helpers"
license = "GPL-2.0-or-later"
publish = false

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]

[features]
default = []
