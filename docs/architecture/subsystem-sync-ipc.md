# Subsystem: IPC / Sync / POSIX

**Files:** `core/sync.inc`, `gui/event.inc`, `memory.inc` IPC, `clipboard.inc`, `posix/*`.  
**Public:** syscalls 54/60/77; sync/event exports for drivers.  
**Objects:** MUTEX, RWSEM, FUTEX, PIPE, EVENT, SMEM.
